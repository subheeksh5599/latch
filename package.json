{
  "name": "provewall",
  "version": "0.1.0",
  "private": true,
  "description": "A page that refuses to go live until a real browser proves its button works.",
  "license": "MIT",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "provewall": "node --env-file-if-exists=.env --import tsx scripts/provewall.ts",
    "reset": "node --import tsx scripts/reset.ts",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "dependencies": {
    "next": "14.2.5",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@types/node": "20.14.0",
    "@types/react": "18.3.3",
    "@types/react-dom": "18.3.0",
    "tsx": "4.16.2",
    "typescript": "5.5.4"
  }
}
